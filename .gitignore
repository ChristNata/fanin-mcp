# Rust
/target/
**/*.rs.bk
*.pdb

# Cargo.lock is committed — fanin-mcp ships a binary (see STACK.md). Do not ignore.

# Covenant orchestration
.covenant-worktrees/

# IDE
.idea/
.vscode/

# Obsidian vault — ignore volatile workspace/cache, keep shared notes + settings
docs/.obsidian/workspace*.json
docs/.obsidian/cache
# glasswing/Obsidian soft-delete lands trashed notes here (vault-root .trash)
docs/.trash/

# OS
.DS_Store
Thumbs.db

# Local logs (fanin-mcp writes only via --log-file, but guard ad-hoc runs)
*.log

# Covenant dispatch capture — post-mortem NDJSON, not a plan artifact
docs/master-plans/**/dispatch-*.ndjson
