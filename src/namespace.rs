//! Namespace ACL — scopes a session's visible tools.
//!
//! Stub module. Future phases enforce the ACL that becomes the real security
//! gate (D-006: annotations are conservative, not a security boundary).
