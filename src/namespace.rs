//! Namespace ACL — scopes a session's visible tools.
//!
//! P0.1: stub. P0.2 carries the selected namespace value through the CLI;
//! later phases enforce the ACL that becomes the real security gate
//! (D-006: annotations are conservative, not a security boundary).